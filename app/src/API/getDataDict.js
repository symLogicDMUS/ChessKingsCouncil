import * as firebase from "firebase/app";
import "firebase/auth";
import "firebase/firestore";

export function getDataDict(username) {
    
    return null;

    // var dataDict = firebase.database().ref('game names').once('symLogicDMUS').then(function(snapshot) {
    //     var gameNames = (snapshot.val())
    //     return gameNames;
    // })

    // return dataDict;
}