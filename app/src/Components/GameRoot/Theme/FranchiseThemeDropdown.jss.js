import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    franchise_theme: props => ({
       marginTop: 'auto',
       marginBottom: 'auto',
    }),
}, {index: 1});