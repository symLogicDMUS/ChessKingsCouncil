import {makeStyles} from "@material-ui/core/styles"

export const useStyles = makeStyles({
    piece: {
        position: 'absolute',
        zIndex: '1',
        width: '12.5%',
        height: '12.5%'
    }
});