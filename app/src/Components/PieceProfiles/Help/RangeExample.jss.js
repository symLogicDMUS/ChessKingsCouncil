import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(
    {
        img: (props) => ({
            width: "100%",
            height: "100%",
        }),
    },
    { index: 1 }
);
