import makeStyles from "@material-ui/core/styles/makeStyles";

export const useStyles = makeStyles({
    piece: {
        position: 'absolute',
        zIndex: 'inherit',
        width: '100%',
        height: '100%',
    }
})