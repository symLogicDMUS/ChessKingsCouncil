import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    button1: props => ({
        marginRight: '1.5vw',
    }),
    button2: props => ({
       marginLeft: '1.5vw',
    }),
}, {index: 1});