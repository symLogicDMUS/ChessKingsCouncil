import {
    playAsDropdownWidth,
    playAsDropdownHeight,
    playAsDropdownTop,
    playAsDropdownLeft,
} from "../../sizeAndPosVariables.jss";

export const playAsDropdown = {
    position: "absolute",
    playAsDropdownWidth: playAsDropdownWidth,
    playAsDropdownHeight: playAsDropdownHeight,
    playAsDropdownTop: playAsDropdownTop,
    playAsDropdownLeft: playAsDropdownLeft,
};