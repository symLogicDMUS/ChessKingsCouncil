import { customizeWidth, customizeHeight, customizeTop, customizeLeft } from "../sizeAndPosVariables.jss";

export const customize = {
    zIndex: 5,
    position: "absolute",
    border: "1px solid #707070",
    customizeWidth: customizeWidth,
    customizeHeight: customizeHeight,
    customizeTop: customizeTop,
    customizeLeft: customizeLeft,
};