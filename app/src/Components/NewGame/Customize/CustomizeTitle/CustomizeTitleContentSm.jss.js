import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    content: props => ({
        width: '100%',
        display: 'flex',
        flexDirection: 'row',
        flexWrap: 'nowrap',
        alignItems: 'center',
    }),
}, {index: 1});