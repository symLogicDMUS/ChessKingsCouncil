import { profileHeaderWidth, profileHeaderHeight } from "../../sizeAndPosVariables.jss";

export const profileHeader = {
    zIndex: "inherit",
    backgroundColor: "#515151",
    profileHeaderWidth: profileHeaderWidth,
    profileHeaderHeight: profileHeaderHeight,
};