import { profileGridTop, margin } from "../../sizeAndPosVariables.jss";

export const profileGrid = {
    position: "relative",
    display: "inline-grid",
    columnGap: margin,
    profileGridTop: profileGridTop,
};

