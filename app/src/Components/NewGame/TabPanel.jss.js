import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
    content: props => ({
        // position: 'fixed',
        padding: '7.5vw',
        height: '100%',
        width: '100%',
        display: 'flex',
        flexDirection: 'column',
    }),
}, {index: 1});