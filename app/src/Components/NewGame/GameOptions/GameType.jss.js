import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(
    {
        game_type: (props) => ({
            width: "35%",
            cursor: "pointer",
        }),
    },
    { index: 1 }
);
