import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
    arrowChunk: (props) => ({
        width: '70%',
        height: '70%',
        margin: 'auto'
    }),
}), { index: 1 });