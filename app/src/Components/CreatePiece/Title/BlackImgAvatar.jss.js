import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(
    {
        avatar: (props) => ({
            marginRight: "1rem",
        }),
    },
    { index: 1 }
);
