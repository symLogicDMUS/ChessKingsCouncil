import {makeStyles} from "@material-ui/core/styles";

export const useStyles = makeStyles({
    backdrop: {
        position: 'absolute',
        top: 0,
        left: 0,
        zIndex: 200,
    }
}, {index: 1});