import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(
    {
        inline_link: (props) => ({
            display: "inline",
        }),
    },
    { index: 1 }
);
