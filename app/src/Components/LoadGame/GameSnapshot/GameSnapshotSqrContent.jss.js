import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(
    {
        sqr_content: (props) => ({
            width: "95%",
            height: "95%",
        }),
    },
    { index: 1 }
);
