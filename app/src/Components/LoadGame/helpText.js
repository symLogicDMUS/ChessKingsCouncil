import React from "react";

export let titles = {
    0: "Load a previously saved game",
};

export let paragraphs = {
    0: (
        <p>
            Currently, all Game categories (Standard, Custom, and Council) are given in 1 dropdown menu.
        </p>
    ),
};

export let children = {
    0: null,
};

export let numSlides = 1;
