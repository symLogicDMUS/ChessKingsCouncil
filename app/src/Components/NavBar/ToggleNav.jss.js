import makeStyles from "@material-ui/core/styles/makeStyles";

export const useStyles = makeStyles({
    toggle_nav: {
        height: "100%",
        MsGridColumn: "10",
        gridColumn: "10",
        zIndex: "1",
        cursor: "pointer",
    },
    widget: {
        width: "100%",
    },
});
