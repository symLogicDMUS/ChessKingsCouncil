import {createMuiTheme, makeStyles} from "@material-ui/core/styles";

export const themes = createMuiTheme({
    dark: {
        text: '#a9a9a9',
        outline1: '#707070',
        fill: '#515151',
        checkmark: '#a9a9a9'
    }
});
