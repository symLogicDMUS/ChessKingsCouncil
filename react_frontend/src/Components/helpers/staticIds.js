/**
 * Ids of the standard pieces that can't be used for custom pieces. capitals not included but checked in program.
 */
export const fenIds = ['k', 'q', 'n', 'b', 'r', 'p'];

/**Ids for black pieces not in list but protected in program*/
export const pieceIds = ['WK', 'WQ', 'WN', 'WB', 'WR', 'WP'];

