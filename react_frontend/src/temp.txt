
class BoardIds {
   constructor(gameName) {
     this.board = {};
     this.isLegal = false;
   }

   update(newBoard) {
     this.board = newBoard
     return;
   }

   moveStatus(isLegal) {
     this.isLegal = isLegal;
     return;
   }
}
 //TODO: eventually the user will pick a saved game or new game option and this will be passed to BoardIds
 let board = new BoardIds("New");