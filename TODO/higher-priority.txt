medium:
* crashes loading Customize page
* Crashes on multiple pages and in multiple places because invalid hook call. happens in components that render lists of components
  see if is because need a unique 'id' prop for each component in list
* crashes loading help modal
* 'explore without account' text button not working

long:
* Add Mobile version of the Board, Square, and Piece components, that uses the Mobile version of React Dnd.